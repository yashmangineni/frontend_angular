export interface QA {
  q: string;
  a: string;
}

export interface Template {
  id: string;
  name: string;
  description: string;
  category: string;
  downloadUrl: string;
  previewImage: string;
  format: string;
}

export interface ResumeTip {
  title: string;
  description: string;
  icon: string;
}